{
	"bugs": "https://github.com/cedx/which.js/issues",
	"description": "Find the instances of an executable in the system path. Like the `which` Linux command.",
	"homepage": "https://docs.belin.io/which.js",
	"license": "MIT",
	"name": "@cedx/which",
	"repository": "cedx/which.js",
	"type": "module",
	"version": "8.0.2",
	"author": {
		"email": "cedric@belin.io",
		"name": "Cédric Belin",
		"url": "https://belin.io"
	},
	"bin": {
		"which": "./bin/which.js"
	},
	"devDependencies": {
		"@types/node": "^20.8.10",
		"c8": "^8.0.1",
		"eslint": "^8.52.0",
		"typedoc": "^0.25.3",
		"typescript": "^5.2.2"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"exports": {
		"types": "./lib/index.d.ts",
		"import": "./src/index.js"
	},
	"files": [
		"lib/",
		"src/"
	],
	"imports": {
		"#which": {
			"types": "./lib/index.d.ts",
			"import": "./src/index.js"
		}
	},
	"keywords": [
		"find",
		"path",
		"system",
		"utility",
		"which"
	],
	"scripts": {
		"build": "tsc --project src/jsconfig.json",
		"clean": "node scripts/clean.js",
		"doc": "typedoc --options etc/typedoc.json && node scripts/doc.js",
		"dist": "npm run clean && npm run build",
		"lint": "tsc --project jsconfig.json && eslint --config=etc/eslint.json bin example scripts src test",
		"postpublish": "node scripts/publish.js",
		"prepack": "npm run dist",
		"start": "node bin/which.js --help",
		"test": "c8 --all --include=src/**/*.js --report-dir=var --reporter=lcovonly node --test --test-reporter=spec"
	}
}
